syntax = "proto3";

package rpc;

service Replicator {
  rpc replicateMsg (Request) returns (Response) {}
}

message Request {
  string msg = 1;
}

// do we need this? Mb gRPC uses ACKs internally?
message Response {
  bool success = 1;
}